# 試験（現場入力・版156）

アプリ本体（`index.html`）は**ビルド無し・npm無し**のままです。ここに入っているのは
**試験だけ**で、アプリの動きには一切関わりません。

## 用意（1回だけ）
この場所で

    npm install playwright-core

Chromium は環境に入っているものを自動で探します（`/opt/pw-browsers/…`）。
別の場所にあるときは `CHROME=/path/to/chrome` を付けてください。

3つのリポジトリ（antenna_main / antenna_genba / antenna_viewer）が
**隣どうしに置いてある**前提です。別の場所のときは、試験の引数で渡せます。

    node smoke_pcbase.js file:///path/to/antenna_genba/index.html

## 走らせ方

    ./全件.sh            # まとめて。結果は 全件.out
    node smoke_pcbase.js # 1本だけ

| ファイル | 見ているもの |
|---|---|
| `smoke_pcbase.js` | 版156 の決まりそのもの（控えで3つ見比べる・写真・完了の取り消し） |
| `smoke_v156_ui.js` | 画面がふつうに動くか（節・入力・工程・台帳・保存バー・保存） |
| `smoke_v156_save.js` | 1件ずつの保存の4つの道（フォルダ・窓・共有・ダウンロード） |
| `smoke_v156_bulk.js` | まとめて保存・全部保存し直す（ふるいと知らせ） |
| `smoke_v156_guard.js` | PCに中身がある戸別の【見るだけ】の関門・台帳との往復・欄の突き合わせ |
| `smoke_v157_genba.js` | 版157 の現場入力（読み合わせられないときは書かない・フォルダの許可切れ・共有で渡した .json.txt・まとめて保存の知らせ・特別記録の読み返し） |
| `smoke_v157_main.js` | 版157 のメイン（アパートの説明欄の気づき・共用部ボックスが金額に入る条件・台帳／Excel／印刷の「ビラ済」） |
| `smoke_v158_main.js` | 版158 のメイン（🧹 の控えフォルダを読まない・同じ管理番号の予定はカレンダーで1件にまとめて ×N） |
| `smoke_v158_genba.js` | 版158 の現場入力（書いたあとの読み返しが一瞬読めなかったときだけ、少し待って1回読み直す） |
| `smoke_v159_main.js` | 版159 のメイン（戸別ファイルへ書き戻すときの写真＝和集合・サムネで上書きしない・読めなければ書かない） |
| `smoke_v159_genba.js` | 版159 の現場入力（写真の控えが失敗したら知らせる・下書きを消されにくくする申し出） |
| `smoke_v160_main.js` | 版160 のメイン（46CH系統のラインブースター＝別の行・46CHアンテナが無ければ計上しない・図面の箱と線） |
| `smoke_v160_genba.js` | 版160 の現場入力（46CHアンテナのときだけ出る欄・既定は「なし」・PCへ渡る） |
| `smoke_v161_main.js` | 版161 のメイン（写真を入れるとき、約1MBを超えるものだけ長辺2048pxへ自動で軽くする） |
| `smoke_v161_genba.js` | 版161 の現場入力（撮る／選ぶ／特別記録の写真も同じように自動で軽くする） |
| `smoke_v162_main.js` | 版162 のメイン（受付台帳から「📉 写真を軽くする」でまとめて縮める。書く→読み返す→確かめる） |
| `smoke_v163_main.js` | 版163 のメイン（縮めた写真に撮影日時・GPSを引き継ぐ。向きの印は「そのまま」に直す） |
| `smoke_v163_genba.js` | 版163 の現場入力（同じ。撮る／選ぶで撮影日時が残る） |
| `smoke_v164_genba.js` | 版164 の現場入力（📂 をまとめて選んで絞り込む・入れる場所の名前を候補から選ぶ） |
| `smoke_v165_main.js` | 版165 のメイン（保安器の所の既設2分配器＝費用なし・黒／写真に日時を足しても1MBを超えない） |
| `smoke_v165_genba.js` | 版165 の現場入力（既設2分配器を選べる・絞り込みの索引は1回・「自分で書く」はアプリの中の窓） |
| `smoke_v166_genba.js` | 版166 の現場入力（上の帯が横にはみ出さない＝画面が横に動かない・縮まない） |
| `smoke_v167_genba.js` | 版167 の現場入力（字の広い端末でも上部がはみ出さない＝折り返す／予定カレンダーの行が切れる） |
| `smoke_v168_genba.js` | 版168 の現場入力（PCで開くと、掴んでいるフォルダから 管理番号.json をその場で読む＝手で探さない） |
| `smoke_v169_genba.js` | 版169 の現場入力（管理番号をひと押しでコピー＝スマホのファイル選択の検索に貼り付けて探す） |
| `smoke_v170_genba.js` | 版170 の現場入力（1つだけ選ぶ／まとめて選ぶ を分け、まとめて選んだらその戸別だけを出す） |
| `smoke_v172_genba.js` | 版172 の現場入力（保存先を「物件 ＞ リスト」に固定する・ルートにできた写しの写真をリストへ和集合で入れる（消さない）・受付台帳を取り込むと入れる場所が決まる・保存バーが横にはみ出さない） |

## 変異試験（壊すと赤くなるか）

    python3 変異/版156.py          # 全件（版156 の決まり・現場入力）
    python3 変異/版156.py 写真      # 名前に「写真」が入るものだけ
    python3 変異/版157.py          # 版157 の直し（現場入力とメインの両方）
    python3 変異/版157.py ビラ      # 名前に「ビラ」が入るものだけ
    python3 変異/版158.py          # 版158 の直し（メインと現場入力）
    python3 変異/版159.py          # 版159 の直し（メインと現場入力）
    python3 変異/版160.py          # 版160 の直し（メインと現場入力）
    python3 変異/版161.py          # 版161 の直し（メインと現場入力）
    python3 変異/版162.py          # 版162 の直し（メインと現場入力）
    python3 変異/版163.py          # 版163 の直し（メインと現場入力）
    python3 変異/版164.py          # 版164 の直し（現場入力）
    python3 変異/版165.py          # 版165 の直し（メインと現場入力）
    python3 変異/版166.py          # 版166 の直し（現場入力）
    python3 変異/版167.py          # 版167 の直し（現場入力）
    python3 変異/版168.py          # 版168 の直し（現場入力）
    python3 変異/版169.py          # 版169 の直し（現場入力）
    python3 変異/版170.py          # 版170 の直し（現場入力）
    python3 変異/版171.py          # 版171 の直し（版166〜170 の見直しで見つかった抜け）
    python3 変異/版172.py          # 版172 の直し（保存先を「物件 ＞ リスト」に固定する）

1箇所ずつ壊した写しを作って試験を走らせ、**赤くなること**を確かめます。
直したのに試験が通るままなら、その試験は効いていません。

## 構文チェック

    python3 syn.py ../../index.html
